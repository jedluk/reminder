const mocha = require('mocha');
const assert = require('chai').assert;
const { isFriday } = require('../../helpers/hbs');

describe('handlebars helper methods', () => {
  it('should be true', () => {
    const id = 1;
    assert.equal(1,1);
  });
})