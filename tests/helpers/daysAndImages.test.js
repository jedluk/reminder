const mocha = require('mocha');
const assert = require('chai').assert;
const { getImgNames, randomNoRepeats, prepareDayStructure } = require('../../helpers/daysAndImages');

describe('day and images helpers methods', () => {
  it('should be true', () => {
    assert.equal(1,1);
  })
})